import Typography from 'typography';

const theme = {
  title: 'goodfries',
  googleFonts: [
    {name: 'Fredoka One', styles: ['400']}
  ]
};

const typography = new Typography(theme);

export default typography;